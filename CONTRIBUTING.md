# Contributing

Long Gate prefers integration over reinvention.

Before adding a new privacy algorithm, first ask whether a mature upstream project already implements it. New integrations should normally be adapters with lazy imports and explicit license notes.

Pull requests that change release policy must include tests proving the default-deny behavior is preserved.
